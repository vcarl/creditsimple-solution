[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "localhost"]
	uri = db:pg://postgres@localhost/"db/development"
[engine "pg"]
	target = localhost
